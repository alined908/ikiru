import { Wrapper, InnerWrapper } from "../components/layout/common";

const ConnectSection = () => {
    return (
      <Wrapper>
        <InnerWrapper>

          Connect
        </InnerWrapper>
        
      </Wrapper>
    )
  }

export default ConnectSection;